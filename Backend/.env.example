# Copia in .env e compila con i tuoi valori per il deploy.
# Non committare .env (contiene segreti).

# === JWT ===
JWT_SECRET=genera-una-chiave-sicura-di-almeno-32-caratteri
JWT_EXPIRATION_MS=21600000

# === Admin iniziale (primo avvio) ===
ADMIN_INIT_USERNAME=admin
ADMIN_INIT_EMAIL=admin@example.com
ADMIN_INIT_PASSWORD=password-provvisoria-da-cambiare

# === Database ===
# Locale: SQLite (DB_URL). Se DATABASE_URL non è impostata, usa SQLite.
# Render/Railway: imposta DATABASE_URL (postgresql://user:pass@host:port/db) → PostgreSQL.
DB_URL=jdbc:sqlite:../Database/portfolio.db
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# === CORS (URL dei frontend) ===
# Se vuoto, usa FRONTEND_URL + ADMIN_FRONTEND_URL
# CORS_ALLOWED_ORIGINS=https://tuosito.com,https://admin.tuosito.com
FRONTEND_URL=https://tuosito.com
ADMIN_FRONTEND_URL=https://admin.tuosito.com

# === Email (notifiche contatti, reset password) ===
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=tuo@email.com
SPRING_MAIL_PASSWORD=tua-app-password
SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
APP_CONTACT_NOTIFICATION_EMAIL=notifiche@tuodominio.com
PASSWORD_RESET_EMAIL_ENABLED=true
